# Examples

Run `generate_synthetic_fixtures.py` from the repository root to create three
deterministic WAV files plus a provenance manifest:

```bash
python examples/generate_synthetic_fixtures.py
```

The output directory is ignored by Git. It contains only generated waveforms;
no microphone or third-party recordings are used.
